{
  "transforms": [
    {
      "mode": "sql",
      "include": "jestest/test.sql",
      "emitTemplate": "{{dir}}/{{name}}.queries.ts"
    }
  ],
  "srcDir": "./src/",
  "db": {
    "dbUrl": "postgres://postgres:password@db/postgres"
  }
}
